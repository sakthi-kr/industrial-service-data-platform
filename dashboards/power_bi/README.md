# Power BI report assets

This directory contains the report theme, DAX measures, model relationship, and visual
specification used to build the Power BI report.

The `.pbix` working file is intentionally excluded from Git because it is a large binary file.
Keep it under `C:\Users\admin\Downloads\Seimens\power-bi-working` and attach it to a GitHub
release after the repository is finalized.

Track only the sanitized report evidence:

- `screenshots/service_operations_overview.png`
- `screenshots/asset_customer_analysis.png`
- `exports/industrial_service_dashboard.pdf`
